database:
  connection:
    host: "localhost"
    port: 27017
    username: ""  # Leave empty if no authentication is required
    password: ""  # Leave empty if no authentication is required
  settings:
    name: "medicine_dispenser"  # Your database name
    auth_source: "admin"